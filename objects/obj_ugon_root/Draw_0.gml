/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();
if(grab_state = Grab_States.Tug)
{
	debug_bar_draw(tug_progress, tug_resist);
}